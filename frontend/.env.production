# Set to the deployed API origin at build/deploy time
# (see docs/production-roadmap.md §9). Left as a same-origin /api default so a
# production build doesn't accidentally point at localhost.
VITE_API_BASE_URL=/api
